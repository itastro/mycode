package com.bailian.car.beans;

import java.util.Comparator;

import com.bailian.car.domain.system.Menu;

public class MenuComparator implements Comparator<Menu> {
	@Override
	public  int compare(Menu m1, Menu m2) {
		return Integer.parseInt(m1.getPriority()) - Integer.parseInt(m2.getPriority());
	}
}
